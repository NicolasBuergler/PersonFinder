import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'person-finder-pwa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
